@model Models.Branch

@{
    ViewData["Title"] = "Nuova Sede";
}

<div class="row justify-content-center">
    <div class="col-md-6">
        <nav aria-label="breadcrumb">
            <ol class="breadcrumb">
                <li class="breadcrumb-item"><a asp-action="Index">Sedi</a></li>
                <li class="breadcrumb-item active" aria-current="page">Nuova Sede</li>
            </ol>
        </nav>

        <div class="card shadow-sm border-0">
            <div class="card-header bg-primary text-white py-3">
                <h4 class="mb-0"><span class="material-symbols-outlined me-2">add_circle</span>Nuova Sede</h4>
            </div>
            <div class="card-body p-4">
                <form asp-action="Create">
                    <div class="mb-3">
                        <label asp-for="Name" class="form-label fw-bold">Nome Sede</label>
                        <input asp-for="Name" class="form-control form-control-lg" placeholder="Es. Sede Centrale" />
                        <span asp-validation-for="Name" class="text-danger small"></span>
                    </div>
                    <div class="mb-3">
                        <label asp-for="Address" class="form-label fw-bold">Indirizzo</label>
                        <textarea asp-for="Address" class="form-control" rows="3" placeholder="Indirizzo completo"></textarea>
                        <span asp-validation-for="Address" class="text-danger small"></span>
                    </div>
                    <div class="d-grid gap-2 d-md-flex justify-content-md-end mt-4">
                        <a asp-action="Index" class="btn btn-light px-4">Annulla</a>
                        <button type="submit" class="btn btn-primary px-5 fw-bold">Salva</button>
                    </div>
                </form>
            </div>
        </div>
    </div>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
